spring:
  datasource:
    url: jdbc:mysql://localhost:3306/liferaydb
    username: liferay
    password: liferay
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8085
