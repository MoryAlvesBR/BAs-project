// src/types/Product.ts (Opcional, mas recomendado para projetos maiores)

export interface Product {
  id: number;
  name: string;
  price: number;
  description: string;
  // Adicione outros campos que vierem do seu backend
}